<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools" tools:context=".MainActivity">
    <item android:id="@+id/share_title"
        android:title="@string/share_string"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/more_title"
        android:title="@string/more_string"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/licence_title"
        android:title="@string/licence_string"
        android:orderInCategory="100"
        app:showAsAction="never" />

    <item android:id="@+id/about_title"
        android:title="@string/about_string"
        android:orderInCategory="100"
        app:showAsAction="never" />
</menu>

